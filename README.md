# Web Mail Client

Monorepo for the Web Mail Client project.

## Structure

- `backend/` contains the Spring Boot backend.
- `frontend/` contains the Angular frontend.

## Run Backend

```sh
cd backend
./gradlew bootRun
```

## Run Frontend

```sh
cd frontend
npm install
npm start
```
